version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - cd backend && npm install --production
run:
  command: node backend/server.js
  network:
    port: 4000
    env: PORT
  env:
    - name: NODE_ENV
      value: "production"
